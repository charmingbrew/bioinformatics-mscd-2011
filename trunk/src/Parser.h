#ifndef _PARSER_H
#define _PARSER_H

#include <string>
using namespace std;

class Parser
{
	public:
		void ParseNPrint(char * Path);
//		Sequence* ParseToSequence(char * Path);
};

#endif
